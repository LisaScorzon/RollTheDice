// import { ValidatorConstraint, ValidatorConstraintInterface } from 'class-validator'
 //import { Score, Symbol} from './entities'




//export const calculateWinner = (score :Score)
   // if total dice of playX >= 18- declare winner

//export const finished = (score: Score)
  // if total dice of playX >= 18- declare finished
